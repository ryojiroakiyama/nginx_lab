version: '3.7'
services:
  web:
    image: nginx
    ports:
      - "8080:80"
    volumes:
      - "./html:/data/www"
      - "./images:/data/images"
      - "./dir1:/data/dir1"
      - "./nginx.conf:/etc/nginx/nginx.conf"